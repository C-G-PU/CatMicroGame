<Window x:Class="DesktopCat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopCat"
        mc:Ignorable="d"
        Title="DesktopCat"
        Height="150" Width="150"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <!-- Главный контейнер -->
    <Grid Background="Transparent">
        <!-- Временно вместо картинки нарисуем круг как заглушку для котика -->
        <Ellipse Fill="Orange" Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="CatSprite" />
        <TextBlock Text="Meow!" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontWeight="Bold"/>

        <!-- Текст уведомления (облачко) -->
        <Border x:Name="NotificationBubble"
                Background="White"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="10"
                Padding="5"
                Margin="0,0,0,10"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Visibility="Collapsed">
            <TextBlock x:Name="NotificationText" Text="Notification text" MaxWidth="120" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
